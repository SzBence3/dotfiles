#hi
Host tekaku
    HostName ssh.tekaku.hu
    ProxyCommand cloudflared access ssh --hostname %h
    User gergo
    RequestTTY yes
    RemoteCommand /bin/fish #--rcfile <(echo "source ~/.bashrc; tmux new -Asbence; exit") -i

